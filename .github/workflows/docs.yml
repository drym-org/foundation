name: docs

on:
  push:
    branches:
      - main
    paths:
      - 'finance/**'
      - '.github/workflows/docs.yml'

defaults:
  run:
    shell: bash

jobs:
  deploy-docs:
    runs-on: ubuntu-latest
    name: Build and deploy financial model documentation
    steps:
      - name: Checkout
        uses: actions/checkout@master
      - name: Install Racket
        uses: Bogdanp/setup-racket@v1.8.1
        with:
          architecture: 'x64'
          distribution: 'full'
          variant: 'CS'
          version: 'stable'
      - name: Install Documentation and its Dependencies
        run: make install
      - name: Build docs for hosting
        run: make build
      - name: Push to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4.3.3
        with:
          folder: finance/output
          branch: gh-pages
          clean: true
          clean-exclude: benchmarks
